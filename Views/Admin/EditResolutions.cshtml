@model IEnumerable<SitaCptTicketApp.Models.Resolution>

@{
    ViewData["Title"] = "Edit Resolutions";
}

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - SitaCptTicketApp</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@@fortawesome/fontawesome-free@6.4.2/css/all.min.css" />
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;500;700&display=swap" rel="stylesheet" />
</head>
<body>
<main>
    <div class="main-container">
        <div class="dashboard-content">
            <h1 class="page-title">Edit Resolutions</h1>

            @if (TempData["SuccessMessage"] != null)
            {
                <div class="alert alert-success">@TempData["SuccessMessage"]</div>
            }
            @if (TempData["ErrorMessage"] != null)
            {
                <div class="alert alert-danger">@TempData["ErrorMessage"]</div>
            }

            <div class="card">
                <h2 class="card-title">Resolutions Table</h2>
                <div class="card-actions">
                    <a asp-action="EditResolution" class="btn btn-sita">Add New Resolution</a>
                </div>
                @if (Model != null && Model.Any())
                {
                    <table class="resolutions-table">
                        <thead>
                            <tr>
                                <th>ID</th>
                                <th>Name</th>
                                <th>Actions</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var resolution in Model.OrderBy(r => r.Name))
                            {
                                <tr>
                                    <td>@resolution.Id</td>
                                    <td>@resolution.Name</td>
                                    <td>
                                        <a asp-action="EditResolution" asp-route-id="@resolution.Id" class="btn btn-sita btn-small">Edit</a>
                                        <form asp-action="DeleteResolution" method="post" style="display: inline;">
                                            <input type="hidden" name="id" value="@resolution.Id" />
                                            <button type="submit" class="btn btn-danger btn-small" onclick="return confirm('Are you sure you want to delete this resolution? This will orphan associated comments.');">
                                                <i class="fas fa-trash"></i>
                                            </button>
                                        </form>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                }
                else
                {
                    <p class="no-data">No resolutions available.</p>
                }
            </div>
        </div>
    </div>
</main>

<style>
    :root {
        --primary: #003087;
        --accent: #0058cc;
        --secondary: #6c757d;
        --danger: #dc3545;
        --text: #333333;
        --text-secondary: #666666;
        --card-bg: #ffffff;
        --background: #f5f7fa;
    }

    body {
        font-family: 'Roboto', sans-serif;
        background: var(--background);
        margin: 0;
        padding: 0;
    }

    .main-container {
        display: flex;
        justify-content: center;
        width: 100%;
        margin: 0 0 40px;
        box-sizing: border-box;
        padding: 20px;
    }

    .dashboard-content {
        width: 100%;
        max-width: 1200px;
        padding: 20px 15px 0;
    }

    .page-title {
        font-size: 32px;
        color: var(--primary);
        margin-bottom: 30px;
        text-align: center;
        font-weight: 700;
    }

    .card {
        background: var(--card-bg);
        border-radius: 8px;
        padding: 20px;
        margin-bottom: 30px;
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.05);
    }

    .card-title {
        font-size: 24px;
        color: var(--primary);
        margin-bottom: 10px;
        text-align: center;
        font-weight: 700;
    }

    .card-actions {
        display: flex;
        justify-content: flex-end;
        margin-bottom: 15px;
    }

    .resolutions-table {
        width: 100%;
        border-collapse: collapse;
        margin-top: 15px;
    }

    .resolutions-table th,
    .resolutions-table td {
        padding: 10px;
        text-align: left;
        border-bottom: 1px solid #e0e7f0;
        font-size: 14px;
    }

    .resolutions-table th {
        background: #e6f0fa;
        color: var(--primary);
        font-weight: 600;
    }

    .resolutions-table td {
        color: var(--text);
    }

    .btn {
        padding: 8px 12px;
        border: none;
        border-radius: 5px;
        cursor: pointer;
        font-size: 14px;
        font-weight: 500;
        text-decoration: none;
        display: inline-flex;
        align-items: center;
        gap: 5px;
        transition: background 0.2s ease;
    }

    .btn-sita {
        background: var(--accent);
        color: #fff;
    }

    .btn-sita:hover {
        background: var(--primary);
    }

    .btn-danger {
        background: var(--danger);
        color: #fff;
    }

    .btn-danger:hover {
        background: #c82333;
    }

    .btn-small {
        padding: 5px 10px;
        font-size: 12px;
    }

    .alert {
        font-size: 14px;
        padding: 10px;
        border-radius: 5px;
        margin-bottom: 15px;
    }

    .alert-success {
        background: #d4edda;
        color: #155724;
    }

    .alert-danger {
        background: #f8d7da;
        color: #721c24;
    }

    .no-data {
        font-size: 14px;
        color: var(--text-secondary);
        text-align: center;
        margin: 20px 0;
    }

    @@media (max-width: 768px) {
        .page-title {
            font-size: 28px;
        }

        .card {
            padding: 15px;
        }

        .resolutions-table th,
        .resolutions-table td {
            font-size: 12px;
            padding: 8px;
        }

        .btn {
            padding: 6px 10px;
            font-size: 12px;
        }
    }
</style>
</body>
</html>